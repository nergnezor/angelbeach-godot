{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cargo build",
      "type": "shell",
      "command": "./build.sh",
      "options": { "cwd": "${workspaceFolder}" },
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": ["$rustc"],
      "presentation": { "reveal": "silent", "panel": "shared" }
    },
    {
      "label": "cargo build --release",
      "type": "shell",
      "command": "./build.sh --release",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": ["$rustc"]
    },
    {
      // No debugger at all. If this runs and "Play (windowed)" does not, the
      // problem is the debug setup, not the game.
      "label": "play (windowed, no debugger)",
      "type": "shell",
      "command": "${config:godotTools.editorPath.godot4} --path ${workspaceFolder} res://src/match/Match.tscn",
      "dependsOn": "cargo build",
      "problemMatcher": [],
      "presentation": { "reveal": "always", "panel": "dedicated" }
    },
    {
      "label": "acceptance: seeded headless match",
      "type": "shell",
      "command": "${config:godotTools.editorPath.godot4} --headless --path ${workspaceFolder} res://src/match/Match.tscn",
      "dependsOn": "cargo build",
      "problemMatcher": [],
      "presentation": { "reveal": "always", "panel": "dedicated" }
    },
    {
      "label": "reimport (after adding a class or an asset)",
      "type": "shell",
      "command": "${config:godotTools.editorPath.godot4} --headless --import --path ${workspaceFolder}",
      "problemMatcher": []
    }
  ]
}
